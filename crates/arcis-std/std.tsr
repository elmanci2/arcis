// arcis-std: top-level entry point.
//
// Re-exports the contents of every other module in the standard library so
// `from std import trim, sum` works without forcing users to learn the
// internal layout.

from strings import trim, upper, lower;
from arrays import sum, average;
from math import PI, E;

export { trim, upper, lower, sum, average, PI, E };
