// Text module: demonstrates `export { name as alias }` (re-export with alias).
export function gritar(s: string): string {
    return s + "!!!";
}

// Re-export the public function `gritar` under the name `gritarFuerte`.
export { gritar as gritarFuerte };